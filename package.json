{
  "name": "munch",
  "version": "1.0.0",
  "description": "Restaurant locator app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "precommit:client": "cd client && yarn precommit",
    "precommit:server": "cd server && yarn precommit",
    "precommit": "npm-run-all precommit:client precommit:server"
  },
  "author": "Ian Banda",
  "license": "ISC",
  "devDependencies": {
    "husky": "^6.0.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {}
}
